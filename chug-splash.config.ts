import { ChugSplashConfig } from './src/chug-splash'

const config: ChugSplashConfig = {
  'My Contract': {
    contractSource: 'SimpleConstructor',
    constructorArgs: {
      _paramA: 1234,
    }
  }
}

export default config
